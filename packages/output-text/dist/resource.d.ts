/**
 * Render a single Resource. Resources only render in `product-dna` style — the
 * other styles are action-shaped and don't fit a structure.
 */
import { Resource, OperationalDna } from './types';
export declare function resourceTitle(r: Resource): string;
export declare function renderResource(r: Resource, op: OperationalDna): string;
//# sourceMappingURL=resource.d.ts.map