{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dna.local/operational/attribute",
  "title": "Attribute",
  "description": "An Attribute is a property that belongs to a Resource. Attributes describe the data shape of a Resource — its fields, their types, and any constraints.",
  "type": "object",
  "required": ["name", "type"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The snake_case name of the attribute.",
      "examples": ["amount", "interest_rate", "status", "published_at", "slug"]
    },
    "type": {
      "type": "string",
      "description": "The data type of the attribute.",
      "enum": ["string", "text", "number", "boolean", "date", "datetime", "enum", "reference"],
      "examples": ["string", "number", "date", "enum"]
    },
    "description": {
      "type": "string",
      "description": "A human-readable explanation of what this attribute represents."
    },
    "required": {
      "type": "boolean",
      "description": "Whether this attribute must be present on the resource.",
      "default": false
    },
    "values": {
      "type": "array",
      "description": "The allowed values when type is 'enum'.",
      "items": { "type": "string" },
      "examples": [
        ["pending", "active", "repaid", "defaulted"],
        ["draft", "published", "archived"]
      ]
    },
    "resource": {
      "type": "string",
      "description": "The referenced Resource name when type is 'reference'.",
      "examples": ["Author", "Department", "Order"]
    }
  },
  "if": { "properties": { "type": { "const": "enum" } } },
  "then": { "required": ["values"] },
  "examples": [
    { "name": "amount", "type": "number", "required": true, "description": "Principal amount of the loan in dollars." },
    { "name": "status", "type": "enum", "required": true, "values": ["pending", "active", "repaid", "defaulted"] },
    { "name": "published_at", "type": "datetime", "description": "When the post was made public." }
  ]
}
