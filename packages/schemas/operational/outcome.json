{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dna.codes/schemas/operational/outcome",
  "title": "Outcome",
  "description": "An Outcome describes what changes when an Operation successfully executes — state transitions, derived updates, and side effects like notifications or downstream operations.",
  "type": "object",
  "required": ["operation", "changes"],
  "properties": {
    "operation": {
      "type": "string",
      "description": "The Operation this outcome applies to, expressed as Target.Action (where Target is any noun primitive).",
      "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$",
      "examples": ["Loan.Approve", "Order.Cancel", "Employee.Terminate"]
    },
    "description": {
      "type": "string",
      "description": "A human-readable explanation of what this outcome produces."
    },
    "changes": {
      "type": "array",
      "description": "Attribute changes applied to the Resource after the operation executes.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["attribute", "set"],
        "properties": {
          "attribute": { "type": "string" },
          "set": {
            "description": "The value to assign. Use 'now' for the current timestamp.",
            "examples": ["active", "now", true, null]
          }
        }
      }
    },
    "initiates": {
      "type": "array",
      "description": "Downstream operations to initiate after this operation completes (intra-domain, synchronous).",
      "items": {
        "type": "string",
        "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$"
      }
    }
  },
  "examples": [
    {
      "operation": "Loan.Approve",
      "changes": [{ "attribute": "status", "set": "active" }]
    },
    {
      "operation": "Order.Cancel",
      "changes": [
        { "attribute": "status", "set": "cancelled" },
        { "attribute": "cancelled_at", "set": "now" }
      ],
      "initiates": ["Payment.Refund"]
    }
  ]
}
