{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dna.codes/schemas/operational/membership",
  "title": "Membership",
  "description": "A Membership is a template-level eligibility statement: 'Persons of type X may hold Roles of type Y, optionally in Groups of type Z.' It captures organizational RBAC at the template level — which kinds of people can fill which kinds of roles in which kinds of groups — without binding specific person-instances. Instance-level (Joe × Father × Family-4271) bindings live in Product/Technical layers.\n\nMembership does not share the noun base shape (no attributes, no actions). It is purely a relationship template.",
  "type": "object",
  "required": ["name", "person", "role"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "The PascalCase name of this Membership template (e.g., EmployeeUnderwriter, PartnerLeadCounsel).",
      "pattern": "^[A-Z][a-zA-Z0-9]*$",
      "examples": ["EmployeeUnderwriter", "PartnerLeadCounsel", "EmployeeAdminWorkspace", "MemberHost"]
    },
    "description": {
      "type": "string",
      "description": "Human-readable explanation of who fills which role where."
    },
    "person": {
      "type": "string",
      "description": "The Person template eligible to hold this Role. References a declared Person.",
      "pattern": "^[A-Z][a-zA-Z0-9]*$",
      "examples": ["Employee", "Partner", "Member", "Customer"]
    },
    "role": {
      "type": "string",
      "description": "The Role template the Person is eligible to hold. References a declared Role.",
      "pattern": "^[A-Z][a-zA-Z0-9]*$",
      "examples": ["Underwriter", "LeadCounsel", "Host", "SuperAdmin"]
    },
    "group": {
      "type": "string",
      "description": "Optional Group template specifying where the Role is exercised. Required when the referenced Role has multi-scope (Role.scope is an array) and disambiguation is needed; optional when Role has a single scope or no scope. The validator resolves this against the Role's scope when both are present.",
      "pattern": "^[A-Z][a-zA-Z0-9]*$",
      "examples": ["BankDepartment", "Case", "Workspace"]
    },
    "domain": {
      "type": "string",
      "description": "The dot-separated domain path this Membership belongs to."
    }
  },
  "examples": [
    {
      "name": "EmployeeUnderwriter",
      "description": "Employees may hold the Underwriter role (in their BankDepartment, inferred from Underwriter.scope).",
      "person": "Employee",
      "role": "Underwriter"
    },
    {
      "name": "PartnerLeadCounsel",
      "description": "Partners may hold the Lead Counsel role on Cases.",
      "person": "Partner",
      "role": "LeadCounsel"
    },
    {
      "name": "EmployeeAdminWorkspace",
      "description": "Employees may hold the SuperAdmin role within Workspaces (disambiguating from Tenant).",
      "person": "Employee",
      "role": "SuperAdmin",
      "group": "Workspace"
    }
  ]
}
