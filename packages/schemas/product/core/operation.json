{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dna.codes/schemas/product/core/operation",
  "title": "Operation",
  "description": "An Operation is a Resource:Action pair — the unit of user/system interaction at the product level. Product Core Operations are surface projections of Operational Operations; the field name is the same across both layers.",
  "type": "object",
  "required": ["resource", "action"],
  "properties": {
    "resource": {
      "type": "string",
      "description": "The Resource this operation belongs to.",
      "examples": ["Loan", "Order", "User"]
    },
    "action": {
      "type": "string",
      "description": "The Action to be performed on the Resource.",
      "examples": ["Apply", "Approve", "Delete"]
    },
    "name": {
      "type": "string",
      "description": "The operation expressed as Resource.Action.",
      "pattern": "^[A-Z][a-zA-Z]*\\.[A-Z][a-zA-Z]*$",
      "examples": ["Loan.Apply", "Loan.Approve", "Order.Ship"]
    },
    "description": {
      "type": "string",
      "description": "A human-readable explanation of what this operation does."
    },
    "changes": {
      "type": "array",
      "description": "State mutations applied to the target Resource when this operation completes. Carried through from the operational layer.",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["attribute", "set"],
        "properties": {
          "attribute": { "type": "string" },
          "set": {
            "description": "The value to assign. Use 'now' for the current timestamp.",
            "examples": ["active", "now", true, null]
          }
        }
      }
    }
  },
  "examples": [
    { "resource": "Loan", "action": "Apply", "name": "Loan.Apply", "description": "A borrower submits a loan application.", "changes": [{ "attribute": "loan.status", "set": "under_review" }] },
    { "resource": "Loan", "action": "Approve", "name": "Loan.Approve", "description": "An underwriter approves a pending loan application.", "changes": [{ "attribute": "loan.status", "set": "active" }] }
  ]
}
