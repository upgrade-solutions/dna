{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Route",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "The URL path for the route"
    },
    "component": {
      "type": "string",
      "description": "The name of the component to render for this route"
    },
    "exact": {
      "type": "boolean",
      "description": "Whether the route should be matched exactly"
    },
    "strict": {
      "type": "boolean",
      "description": "Whether the trailing slash should be taken into account"
    },
    "sensitive": {
      "type": "boolean",
      "description": "Whether the matching should be case sensitive"
    }
  },
  "required": ["path", "component"],
  "additionalProperties": false
}