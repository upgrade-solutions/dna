{
  "key": "Paragraph",
  "properties": {
    "text": {
      "type": "string",
      "description": "The text content of the paragraph"
    },
    "align": {
      "type": "string",
      "enum": ["left", "center", "right", "justify"],
      "description": "Specifies the text alignment"
    },
    "color": {
      "type": "string",
      "description": "The text color, can be a color name, hex, or CSS color value"
    },
    "lineHeight": {
      "type": ["number", "string"],
      "description": "Specifies the line height of the text"
    },
    "className": {
      "type": "string",
      "description": "Optional CSS class for custom styling"
    },
    "onClick": {
      "type": "object",
      "description": "Callback function triggered when the paragraph is clicked"
    }
  }
}
